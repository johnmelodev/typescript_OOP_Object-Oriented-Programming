let firstName: string = "Joao";
let lastName: string;
lastName = "Melo";

// Inferência de tipo e Template String
const fullName = `${firstName} ${lastName}`;

console.log(fullName.toUpperCase()); // "JOÃO MELO"
