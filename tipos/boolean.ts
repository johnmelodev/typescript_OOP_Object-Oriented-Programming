let isActive: boolean = true;
isActive = false; // Permitido mudar para o estado oposto

let isPublished = true; // O TS infere que é boolean automaticamente

// isActive = "sim"; // ❌ ERRO: O TypeScript não aceita texto em booleano
